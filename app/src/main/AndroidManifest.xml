<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/lion_school"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LionSchool"
        tools:targetApi="31">
        <activity
            android:name=".PerformanceStudent"
            android:exported="false"
            android:label="@string/title_activity_performance_student"
            android:theme="@style/Theme.LionSchool" />
        <activity
            android:name=".Students"
            android:exported="false"
            android:label="@string/title_activity_students"
            android:theme="@style/Theme.LionSchool" />
        <activity
            android:name=".Course"
            android:exported="false"
            android:label="@string/title_activity_course"
            android:theme="@style/Theme.LionSchool" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.LionSchool">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>